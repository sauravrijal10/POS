<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data>

        <!-- Template for User Verification Form -->
        <template id="user_verification_form" name="User Verification Form">
            <t t-call="website.layout">
                <div class="container mt32 mb32">
                    <div class="row">
                        <div class="col-12 col-md-6 offset-md-3">
                            <h1>User Verification</h1>
                            <form action="/custom_user/verify" method="post">
                                <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                                <div class="form-group">
                                    <label for="username">Username</label>
                                    <input type="text" name="username" id="username" class="form-control" required="required"/>
                                </div>
                                <div class="form-group">
                                    <label for="password">Password</label>
                                    <input type="password" name="password" id="password" class="form-control" required="required"/>
                                </div>
                                <button type="submit" class="btn btn-primary">Verify</button>
                            </form>
                        </div>
                    </div>
                </div>
            </t>
        </template>

        <!-- Template for POS Web Page -->
        <template id="pos_web_template" name="POS Web">
            <t t-call="website.layout">
                <div class="container mt32 mb32">
                    <div class="row">
                        <div class="col-12">
                            <h1>Welcome to the POS System</h1>
                            <p>POS interface will be here.</p>
                        </div>
                    </div>
                </div>
            </t>
        </template>

    </data>
</odoo>
