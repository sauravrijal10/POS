<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- <record id="custom_view_pos_session_shop" model="ir.ui.view">
        <field name="name">pos.session.shop.custom.view</field>
        <field name="model">pos.session.shop</field>
        <field name="inherit_id" ref="point_of_sale.view_pos_session_shop"/>
        <field name="arch" type="xml">
            Make your customizations here -->
            <!-- For example, you can add a new field or modify existing ones -->
            <!-- <field name="custom_field" position="after">
                <field name="another_custom_field"/>
            </field> -->
        <!-- </field> -->
    <!-- </record>  -->
   
    <record id="action_pos_session_filtered" model="ir.actions.act_window">
        <field name="name">Sessions</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">pos.session</field>
        <field name="view_mode">tree,form</field>
    </record>
        
    <record id="custom_pos_dashboard_view" model="ir.ui.view">
         <field name="name">pos.session.
</odoo>